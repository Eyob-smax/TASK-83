/**
 * Export file generation adapter.
 *
 * <p>Generates CSV export files with watermark injection, writes to
 * configured local storage paths, and enforces download restrictions.</p>
 */
package com.eventops.integration.export;
