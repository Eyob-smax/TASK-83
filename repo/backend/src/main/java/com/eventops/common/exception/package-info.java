/**
 * Global exception handling and custom exception types.
 *
 * <p>Centralizes error normalization into the {@code ApiResponse} envelope.
 * Custom exceptions include resource-not-found, conflict, rate-limit-exceeded,
 * validation failure, and business-rule violations.</p>
 */
package com.eventops.common.exception;
