/**
 * Centralized Spring configuration beans.
 *
 * <p>Contains web configuration (CORS, interceptors, message converters),
 * async/executor thread pool settings, Jackson serialization rules, and
 * application-wide constants. No business logic belongs here.</p>
 */
package com.eventops.config;
