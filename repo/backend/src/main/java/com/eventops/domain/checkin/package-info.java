/**
 * Check-in domain: CheckInRecord, PasscodeState, DeviceBinding entities.
 *
 * <p>Models event-day check-in records, rotating 6-digit passcode state,
 * device binding (optional one-time per day), and concurrent multi-device
 * conflict detection.</p>
 */
package com.eventops.domain.checkin;
