/**
 * Spring Security configuration.
 *
 * <p>Defines the SecurityFilterChain bean, HTTPS/TLS settings, filter
 * ordering (signature verification, rate limiting, authentication,
 * authorization), session policy, and CSRF configuration appropriate
 * for the offline local-network deployment model.</p>
 */
package com.eventops.security.config;
