/**
 * REST API controllers.
 *
 * <p>Controllers are thin: validate input, delegate to services, and wrap
 * responses in the {@code ApiResponse} envelope. Each sub-package exposes
 * endpoints for one functional area.</p>
 */
package com.eventops.controller;
